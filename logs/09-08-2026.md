# 09-08-2026

## Project structure initialization

- Initialized `/home/gargantua/code/claude-project-template` with the claude-project-structure layout.
- Git repo already existed (clean, branch `main`, no commits, no files besides `.git`).
- Asked track-vs-gitignore. Decision: track `CLAUDE.md`, `logs/`, `memories/`, and `notes/` in git. No `.gitignore` created.
- Created `CLAUDE.md` (single pointer line), and empty `logs/`, `memories/`, `notes/`.
- Nothing committed; staging/commit left to the user.

## Bundled skills into the repo

- Copied `documentation` and `claude-project-structure` skills from `~/.claude/skills/` into `.claude/skills/<name>/SKILL.md`.
- Both are single-file skills; no supporting files to copy.
- Scanned both copies for user-specific strings (usernames, emails, home paths, employer). None present, so they are safe for a tracked/public repo.
- These are now forked copies. Edits to the global skills will not propagate here, and vice versa.

## README

- Added `README.md` at the user's request. Covers purpose, layout, bundled skills, and how to use the template.
- Note: the claude-project-structure skill states the layout has no README. That rule governs the Claude-facing structure; this README is human-facing repo documentation for a template repo, added on explicit instruction.

## Skill edit: dropped the no-README gotcha

- The claude-project-structure gotcha read: "This layout has no README.md and no changelog index. Current project state lives in `memories/`, history in `logs/`."
- User judged the README half out of scope for the skill. Removed that clause only; kept the changelog half, which still describes the layout.
- Applied to both copies: `~/.claude/skills/claude-project-structure/SKILL.md` and `.claude/skills/claude-project-structure/SKILL.md`. Verified identical afterward.

## Initial commit

- Committed everything to `main` as `1dc535c`: `CLAUDE.md`, `README.md`, `.claude/skills/` (both skills), `logs/09-08-2026.md`.
- `memories/` and `notes/` are empty, and git does not track empty directories, so they are absent from the commit and will not exist in a clone. Left as-is; the skill says leave them empty with no placeholder files, which conflicts with adding `.gitkeep`. Unresolved, flagged to the user.
- Not pushed; no remote configured.

## .gitkeep resolution

- Resolved the empty-directory issue: added `.gitkeep` to `memories/` and `notes/` so a clone of this template gets both directories.
- This contradicts the claude-project-structure instruction "Leave them empty. No placeholder files." The user chose the `.gitkeep` route anyway; the skill text was not amended, so the conflict still stands in the skill.
- Amended the initial commit rather than adding a follow-up. Safe: nothing was pushed and no remote is configured.

## Skill edit: .gitkeep rule

- Amended setup step 4 in both skill copies (global and repo fork) to resolve the conflict logged above.
- Rule now: leave the directories empty, except add `.gitkeep` to `memories/` and `notes/` when step 2 decided to track them in git. `logs/` is excluded because its first dated entry is written during setup. Nothing added when gitignored or when there is no repo.
- Verified both copies identical.
